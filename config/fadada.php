<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 应用配置
    |--------------------------------------------------------------------------
    |
    | 在法大大开放平台申请应用后获得的 app_id 与 app_secret。
    |
    */
    'app_id'     => env('FADADA_APP_ID', ''),
    'app_secret' => env('FADADA_APP_SECRET', ''),

    /*
    |--------------------------------------------------------------------------
    | 服务地址
    |--------------------------------------------------------------------------
    |
    | 法大大 FASC OpenAPI 服务地址，默认 v5.1。
    |
    */
    'service_url' => env('FADADA_SERVICE_URL', 'https://api.fadada.com/api/v5/'),

    /*
    |--------------------------------------------------------------------------
    | 超时时间（秒）
    |--------------------------------------------------------------------------
    */
    'timeout' => (int) env('FADADA_TIMEOUT', 60),

    /*
    |--------------------------------------------------------------------------
    | 调用方标识（Initiator）
    |--------------------------------------------------------------------------
    |
    | 法大大接口常用参数 Initiator，结构为数组：
    |   - idType      主体类型：corp=企业，person=个人
    |   - openCorpId  当 idType=corp  时的企业主体标识（应用系统上的企业用户）
    |   - openUserId  当 idType=person 时的个人主体标识（应用系统上的个人用户）
    |
    | 业务 Service 中可通过 BaseService::getInitiator() 直接获取该数组。
    |
    */
    'initiator' => [
        'idType'     => env('FADADA_INITIATOR_ID_TYPE', 'corp'), // corp | person
        'openId' => env('FADADA_INITIATOR_OPEN_ID', ''),
    ],

    /*
    |--------------------------------------------------------------------------
    | 调试模式
    |--------------------------------------------------------------------------
    |
    | 开启后会输出 SDK 请求/响应日志，便于排查问题。
    |
    */
    'debug' => (bool) env('FADADA_DEBUG', false),

    /*
    |--------------------------------------------------------------------------
    | AccessToken 缓存
    |--------------------------------------------------------------------------
    |
    | AccessToken 默认缓存在 Laravel cache 中，TTL 默认 7000 秒
    | （法大大 AccessToken 有效期为 2 小时 = 7200 秒，
    |  提前 200 秒过期以避免临界时间点的请求失败）。
    |
    */
    'token' => [
        'cache_store' => env('FADADA_TOKEN_STORE'),   // 留空使用默认 cache store
        'cache_prefix'=> env('FADADA_TOKEN_PREFIX', 'fadada:access_token:'),
        'ttl'         => (int) env('FADADA_TOKEN_TTL', 7000),
    ],
];
